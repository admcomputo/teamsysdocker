services:
  backend:
    build: ./Tis-back
    container_name: tis-back
    restart: always
    ports:
      - "8081:8081"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 3306
      DB_NAME: teamsys_db
      DB_USER: teamsys
      DB_PASSWORD: skfeCISavucTSw6

      BREVO_KEY: TU_CLAVE_REAL_DE_BREVO
      BREVO_EMAIL: 202103868@est.umss.edu
      URL_FRONT: https://teamsys.apps.cs.umss.edu.bo
      JWT_SECRET: una_clave_secreta_muy_larga_y_segura_para_deploy
      PORT: 8081

  frontend:
    build:
      context: ./Tis-front
      args:
        VITE_API_URL: https://teamsysback.apps.cs.umss.edu.bo/api
    container_name: tis-front
    restart: always
    ports:
      - "5173:80"
    depends_on:
      - backend